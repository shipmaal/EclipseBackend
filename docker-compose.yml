services:
  web:
    build: .
    container_name: ${CONTAINER_NAME}
    ports:
      - "${HOST_PORT}:${CONTAINER_PORT}"
    volumes:
      # Persist downloaded SPICE kernels across restarts so they are fetched once.
      - spice-kernels:/app/kernels

volumes:
  spice-kernels:
