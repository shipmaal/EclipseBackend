# Python caches / build artifacts
__pycache__/
*.py[cod]
*.so
*.o
*.a
build/
dist/
.ccache/
compile_commands.json
*.egg-info/

# Virtual envs
.venv/
venv/

# Environment / secrets
.env

# uv keeps uv.lock committed; nothing to ignore there

# SPICE kernels (downloaded via `python -m kernels.bootstrap`)
kernels/*.bsp
kernels/*.bpc
kernels/*.tls
kernels/*.tpc
kernels/*.tm

# Data / scratch output
/data/
*.pdf
app/*.png
app/*.csv
app/*.pstats
restats

# Compiled example binary
c_src/ElpMpp02/myprogram

# Native SPICE toolkit (fetched separately)
/c_src/spice/cspice
/c_src/spice/cspice.tar
/c_src/spice/importCSpice.csh

# IDE
/.vscode/
/.idea/
