// Bindings for eclipse/geometry.hpp (app.geography shadow_edge_limits_v,
// shadow_radii, great-circle helpers, central_track, global_contacts).
// Phase-2 work package C fills this in; the stub keeps the module assembly in
// _eclipse.cpp fixed so the packages stay file-disjoint.
#include "common.hpp"

void bind_geometry(nb::module_& /*m*/) {}
