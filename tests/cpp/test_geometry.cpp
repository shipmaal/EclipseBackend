// Shadow geometry (eclipse/geometry.hpp) — unit tests and parity against the
// ``gc`` / ``lim`` / ``rad`` records of fixtures/geometry_cases.txt and the
// ``contacts`` records of the per-eclipse fixtures.
// Placeholder: phase-2 work package C adds the tests with the header.
